# Copy this file to .env to configure your local GNOMAN 2.0 environment.
# Values shown here reflect sane development defaults.

# Express backend port (backend/index.ts)
PORT=4399

# Electron/renderer build mode toggles (main/main.ts)
NODE_ENV=development

# Vite renderer dev server URL (used by main/main.ts)
VITE_DEV_SERVER_URL=http://localhost:5173

# Default path for the Ed25519 private key used by backend/licenses/gen_license.py
# Override this when issuing licenses so the CLI finds the correct key without needing --priv.
LICENSE_PRIVATE_KEY=backend/licenses/license_private.pem
